{
  "name": "abaplint-to-junit",
  "version": "0.1.0",
  "description": "Convert abaplint reports to junit compatible xml for use with e.g. gitlab ci",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "junit-report-builder": "^1.3.1"
  }
}
